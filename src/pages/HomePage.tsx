import Section from "@/components/Section";

const HomePage = () => {
  return (
    <div>
      <Section park={'Blog'}/>
      <Section park={'GitHub'}/>
      <Section park={'Portfolio'}/>
    </div>
  );
};

export default HomePage;